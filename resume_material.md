# DevForge 简历材料包（按你的要求重构版）

> 全部数字来自仓库真实数据（`benchmarks/results_all.json` / `docs/RESUME.md` / 代码）。
> 无数字处用 `[待补充]` 占位。诚实标注了"项目实际有 / 无"的组件，避免面试被问穿。

---

## 一、项目条目 Bullet Points（3-4 条，动作动词 + 量化结果开头）

**DevForge · 多智能体 AI 软件开发平台｜LLM Agent 编排 · 质量门禁 · 成本工程**（Python/FastAPI · Vue3+TS · WebSocket · ChromaDB · Docker 沙箱 · DeepSeek API）

1. **设计并实现** 6 阶段 × 10+ 角色的多智能体协作流水线（需求→设计→编码→验证→质检→文档），以声明式 `pipeline_spec` 表驱动编排、coder×N / reviewer×4 阶段内并行，单任务全自动交付仅需 10–19 分钟（实测 8/8 任务质检 PASS 且评分 100），较传统人工开发提效 `[待补充]%`。

2. **构建** "契约 + 黑板 + 最小权限"协作体系与两级质量门禁，平台 AST 校验模块契约缺口并强制修复，将 AI 生成代码的**可运行交付率从 20% 提升至 90%**（质检通过率 40%→80%），10 任务平台门禁与外部 golden 客观验证 0 矛盾。

3. **落地** 缓存感知的成本工程：按 LLM 前缀缓存规则（命中价 1/10）重构消息组织，实现 **72–80% 缓存命中率、输入成本降低约 65%、修复成本降低 64–75%**，预算熔断任务从 6/10 降至 0，全局 800k/1.5M token 双熔断兜底。

4. **自建** 评测闭环与全栈工程化：外部固定断言（golden tests）逐任务验证生成代码真实行为，不信任 LLM 自评；沉淀约 12.7k 行 Python + 6.7k 行前端 + 4.3k 行测试、**298 个单元测试**全绿。

---

## 二、代码中最具技术深度的 3 个设计决策

### 决策 1：契约 + AST 平台校验，替代 Agent"自觉"
- 并行 coder 互不可见，接口约定靠自觉必错位。方案：CTO 在设计阶段统一定义**模块接口契约**（exports + 签名），作为唯一标准；平台层 `contract_gap_check()` 用 **AST 解析源码**对比契约导出，编码期"写前校验 + integrator 前强制修复"，质检期复查缺口直接 FAIL 进回跳/升级。
- 工具按角色**白名单硬约束**：`execute` 校验当前 agent 允许工具集，越权直接拒绝；并行 coder 按**模块文件白名单隔离**，只能写自己的文件。
- 核心文件：`src/codegen/domain/contracts.py`、`src/codegen/application/phases/coding.py`（`contract_gap_check`）、`src/codegen/infrastructure/tools/registry.py`。

### 决策 2：三层执行安全——Docker 沙箱 + 静态 AST 扫描 + 运行期 sitecustomize shim
- 首选 Docker 容器（python:3.12-slim）隔离执行，代理免疫、依赖自动安装；Docker 不可用时回退宿主机，叠加两道防线：
  - **静态 AST 扫描分级**：subprocess/shell/动态执行硬拦截，文件操作按路径分级、越项目根拒绝；
  - **运行期沙箱 shim**：通过 `sitecustomize` 注入子进程，patch `os.remove/rename`、`shutil.rmtree/move` 等破坏性操作，目标路径逃逸 {项目根, 系统临时目录} 即抛 `PermissionError`。
- docker 探测结果缓存 + 30s 重试 + 常见安装路径回退，避免环境抖动永久降级。
- 核心文件：`src/codegen/infrastructure/sandbox.py`、`src/codegen/infrastructure/tools/registry.py`（`sandbox_prefix`/`docker_available`）。

### 决策 3：缓存感知的消息组织（成本工程）
- DeepSeek 前缀缓存**命中价约为未命中 1/10**，而"缓存命中 = 消息前缀稳定"。据此制定消息组织规则：稳定 system prompt 置前、易变内容（时间戳/run_id）禁止进前缀、消息只追加不重排、工具循环严格成对、已读拦截不重发文件内容、**上下文压缩说明追加到消息尾部**（保住公共前缀）、工具结果截断 3000 字符。
- 配合阶段 token 预算 + 全局熔断双护栏（默认 800k 预警 / 1.5M 硬停），失败不再无限烧钱。
- 核心文件：`src/codegen/application/chat_chain.py`（压缩策略）、`src/codegen/infrastructure/llm_client.py`、`docs/COST.md`。

---

## 三、3 条 STAR"项目职责与难点"（含量化指标）

### STAR 1｜多 Agent 并行协作一致性
- **情境 S**：多 agent 并行编码互不可见，接口约定靠自觉必错位，模块文件互相覆盖；阶段间自由对话无法校验交付。
- **任务 T**：让 10+ 角色分阶段串行、阶段内并行协作而不错位。
- **行动 A**：设计"契约 + 黑板 + 三层隔离"：CTO 定义模块接口契约作为唯一标准；阶段交接走黑板 + 结构化 JSON + Schema 校验（非法输出丢弃重出）；工具按角色最小权限硬约束、并行 coder 文件白名单隔离；平台 AST 校验契约缺口、编码期强制修复。
- **结果 R**：契约缺口在编码期即被平台检出并强制修复（不再等集成期暴露），模块文件平台级零互相覆盖；10 任务平台门禁与 golden 客观验证 **0 矛盾**。

### STAR 2｜可验证交付与质量门禁
- **情境 S**：AI 生成代码"看着能跑、实际跑不起来"，LLM 自评橡皮图章；修复前可运行交付率仅 **20%**。
- **任务 T**：让"一句话需求"的交付可验证、可审计。
- **行动 A**：构建两级门禁 + 客观评测：静态 AST 门禁（契约/入口接线检查）、动态测试门禁（质检先跑真实测试再判定）、golden 外部断言不信任自评；用评测实测抓出 3 处门禁矛盾（2 处漏检 + 1 处误杀）并修复归零。
- **结果 R**：可运行交付率 **20%→90%**，质检通过率 **40%→80%**；每次改动跑 golden 回归，平台自评可信度量化可查。

### STAR 3｜成本工程与熔断治理
- **情境 S**：多 agent 流水线 token 消耗失控，10 任务中 **6 个烧穿预算**被熔断。
- **任务 T**：让流水线成本可控、可预测、可度量。
- **行动 A**：按前缀缓存命中规则重构消息组织（稳定前缀命中 1/10 价；已读拦截；压缩说明移尾部保前缀；易变内容不进前缀），叠加阶段 token 预算 + 全局熔断双护栏，并把缓存命中率接入 benchmark 度量。
- **结果 R**：缓存命中率 **72–80%**，输入成本降约 **65%**，修复成本降 **64–75%**，熔断任务 **6→0**。

---

## 四、核心痛点（人工基线）

- **原有人工流程**：一个 CLI/小工具从需求沟通 → 编码 → 自测 → 修 bug → 文档，通常耗时 `[待补充] 小时/天`（面试可按"半天到数天"口径准备）；直接丢给 ChatGPT 单次生成：**无测试、无文档、import 报错、跑不起来**，追问多轮"改一处坏两处"。
- **出错率**：修复前平台实测 AI 生成代码**可运行交付率仅 20%（即约 80% 不可直接运行）**，质检通过率 40%；平台建设后交付率 90%、通过率 80%，且 todo_api 等复杂任务仍有 2 次生成未跑通（门禁正确拦截）。
- **成本痛点**：修复前 10 任务中 6 个烧穿 token 预算被熔断 → 治理后为 0。

---

## 五、系统全景（模块数 / 上下游依赖）

> 诚实说明：**DevForge 是单体多模块架构（DDD 分层），不是微服务**——无独立网关、无 MQ、无多服务实例。以下为真实模块与依赖图；模板里的"微服务/网关/MQ"字段请勿直接照抄，见第六节映射。

| 模块 | 职责 | 技术 |
|---|---|---|
| `src/serving`（接口层） | HTTP API + WebSocket + FIFO 任务队列（单活） | FastAPI / uvicorn / 内存队列 + worker 线程 |
| `src/codegen/application` | 流水线引擎（spec 表驱动）+ 7 个阶段实现 | Python 编排 |
| `src/codegen/domain` | Agent（ReAct 循环）/ Blackboard / 契约 / 端口 | 自研 |
| `src/codegen/infrastructure` | LLM 客户端 / 工具系统（10 个工具）/ Docker 沙箱 + shim | openai SDK / Docker |
| `src/memory` | 跨项目记忆（阶段方案 / 已验证函数 / 修复模式 3 集合） | ChromaDB |
| `src/core` | 配置 / 事件总线 / 日志 / 文本解析 | 自研 |
| `web/` | 实时可视化前端（Dashboard / 像素办公室 / 历史 / 项目详情） | Vue3 + TS + WebSocket |

**上游依赖**：DeepSeek（OpenAI 兼容）LLM API、Docker daemon（可选，不可用自动回退宿主机）。
**下游存储**：ChromaDB（`.memory/chroma`）、文件系统（`WareHouse/<任务>_DevForge_<时间戳>/` 交付目录、`.devforge/` checkpoint/事件/agent 历史）、git（每轮产物自动 commit 可回滚）。
**外部接口**：`POST /api/run`（提交任务）、WebSocket（事件流）、`/api/projects`、`/api/memory`；`auth_token` 中间件守卫所有 /api。

---

## 六、请求全链路（按你的模板重组的真实版）

```
浏览器 (Vue3 前端)
  │  WebSocket 事件流（实时回放/断线可恢复）
  ▼
Nginx / Vite dev server（静态资源托管，生产用 Nginx 反代  [待补充: 真实部署域名/端口]）
  │  HTTP POST /api/run（携带 X-Auth-Token）
  ▼
FastAPI 网关层（auth_token 中间件鉴权 + 路由分发）        ← 模板"网关"
  │
  ▼
FIFO 任务队列（内存队列 + 后台 worker 线程，同一时刻单活）  ← 模板"MQ"的对应物（进程内，非消息中间件）
  │
  ▼
流水线引擎 Pipeline（pipeline_spec 声明式编排 6 阶段）     ← 模板"服务A"
  │  阶段串行；coder×N / reviewer×4 阶段内并行
  ▼
Agent ReAct 循环（10+ 角色，工具白名单硬约束）
  │
  ├──► LLM API（DeepSeek，前缀缓存命中价 1/10）            ← 上游外部依赖
  └──► Docker 沙箱执行（不可用回退宿主机 + AST 扫描 + shim）
  │
  ▼
交付落库（对应模板"服务B→DB"）
  ├── WareHouse/<任务>/ 代码+测试+文档（文件系统，git 自动提交）
  ├── ChromaDB 记忆（跨项目召回，只存验证过的）
  └── .devforge/ checkpoint + 事件增量落盘（断点续跑）
```

**模板字段 → 项目实况对照（面试防穿帮）**：

| 模板字段 | DevForge 实况 |
|---|---|
| Nginx | 本地为 Vite dev server（:5173）；生产反代需 `[待补充]`（项目未部署） |
| 网关 | FastAPI 中间件层（auth_token 鉴权 + 路由），非独立网关服务 |
| MQ | 进程内 FIFO 队列（`run_queue.py`），**不是 RabbitMQ/Kafka**；多实例需外部化队列 `[待补充]` |
| 服务A / 服务B | 无第二服务；"服务B"对应执行层（Docker 沙箱）+ 存储层 |
| DB | ChromaDB（向量）+ 文件系统（交付物）+ SQLite（Chroma 底层） |

---

## 七、架构层面亮点（如实映射）

- **容灾 / 恢复**：阶段 checkpoint + 事件增量落盘（进程被杀不丢运行记录）+ agent 对话历史归档（同阶段重跑自动恢复上下文）；每轮产物 git 自动提交（失败版本可回滚对比）；异常完整 traceback 落 `crash.log` 便于定位。
- **横向扩展**：阶段内 agent 级并行（coder×N 并行写模块、reviewer×4 并行审查）是提速关键；流水线级扩展需任务队列外部化 + 多实例 `[待补充]`（当前为单进程单活 FIFO）。
- **灰度 / 渐进发布**：流水线拓扑由 `pipeline_spec` 声明式配置 → 支持**消融评测**（minimal/default/full 三档，量化"多 agent 各阶段值不值"）；已编码项目可**动态插入 Iterate 阶段**走增量迭代；质检 FAIL 回跳 ≤3 次、同缺口二跳自动升级重设计（fixer 修不动=架构问题，反馈喂 CTO）。
- **成本护栏**：阶段 token 预算 + 全局熔断双护栏（800k 预警 / 1.5M 硬停）。

---

## 八、"项目背景与成果"段落（专业语气，突出降本增效）

> 数值留空处用 `[具体数字]` 待你填写。

**项目背景**：随着 AI 编码工具普及，直接将需求交给通用大模型存在"生成 ≠ 交付"的根本问题——产出代码普遍缺少测试与文档、无法直接运行，多轮追问后"改一处坏两处"，且多智能体协作时接口错位、文件互相覆盖，成本失控。为将 AI 软件开发从"单次生成"升级为"可验证、可审计、成本可控的工程流水线"，我独立设计并实现了 DevForge 多智能体 AI 软件开发平台。

**核心成果**：平台以 6 阶段 × 10+ 角色的协作流水线（需求澄清→架构设计→并行编码→测试审查→质量门禁→文档）实现"一句话需求 → 可运行项目"的全自动交付，单任务交付耗时压缩至 `[具体数字] 分钟`，较人工开发效率提升 `[具体数字] 倍`；通过"契约 + 黑板 + 最小权限"协作体系与两级质量门禁，将 AI 生成代码的可运行交付率由 20% 提升至 90%，质检通过率由 40% 提升至 80%；依托缓存感知的消息组织与预算熔断双护栏，LLM 输入成本降低约 65%、修复成本降低 64–75%，预算熔断任务由 6 个降至 0，整体 LLM 调用成本下降 `[具体数字]%`。平台沉淀 298 个单元测试与 10 任务 golden 评测体系，实现"每次改动可回归、每分钱花在明处"。

---

## 九、3 个"基于 / 利用"强动词短语（专业技能栏）

1. **基于**"契约 + 黑板 + 最小权限"协作模型，实现 10+ 角色多智能体的并行编码编排与平台级接口契约校验（AST 缺口自动修复）。
2. **基于** LLM 前缀缓存命中机制重构消息组织，结合阶段预算与全局熔断，将多智能体流水线输入成本降低约 65%。
3. **利用** AST 静态扫描与 Docker/宿主机双模沙箱（运行期 sitecustomize shim），构建生成代码的三层执行安全与隔离体系。
