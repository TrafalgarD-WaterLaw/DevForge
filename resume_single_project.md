# DevForge · 完整单一项目简历描述（v3 术语通俗化版）

> 按"STAR + 技术分层"重构：4 个要点 = 4 个独立技术攻坚方向，动词开头、数据收尾。
> v3 已把代码内部术语（pipeline_spec / coder×N / golden / sitecustomize shim）替换为
> 通用工程用语，另附"术语对照表"供面试展开。
> 末尾附埋点对照表 + 数据口径备忘。

---

## 【项目描述】

**DevForge · 多智能体 AI 软件开发平台** — 面向**非确定性自然语言需求**，以 10+ 角色协作流水线将"一句话需求"端到端交付为可运行项目，10 任务首次编译通过率 90%。

## 【核心技术栈】（按层分组）

- **智能体编排**（核心自研，不依赖现成 Agent 框架）：ReAct 工具循环 · 工具注册调度系统 · 6 阶段 × 10+ 角色 · 声明式流水线编排配置 · ChromaDB 跨项目记忆
- **后端工程**：Python 3.10+ · FastAPI · WebSocket · AST 静态分析 · Docker 沙箱 · pytest（298 用例）
- **前端可视化**：Vue 3 + TypeScript · WebSocket 实时事件流渲染

## 【专业技能】

1. **AI / LLM 应用开发**：从零实现多智能体 Agent 系统——自研 ReAct 工具循环与 10 个工具注册调度（按角色最小权限硬约束），深入掌握 prompt 工程、工具调用、上下文压缩与 LLM 缓存计费优化（缓存命中率 72–80%、输入成本降约 65%）。
2. **后端与并发**：精通 Python / FastAPI / WebSocket，独立实现事件驱动架构、FIFO 任务队列与多线程并行调度（单阶段 4–8 智能体并发），具备线程安全（锁保护计数）、进程安全与崩溃恢复（checkpoint + 事件增量快照）实战经验。
3. **架构设计**：熟练运用 DDD 分层（domain / application / infrastructure / interfaces）与声明式编排（配置驱动流水线，改配置不改代码），设计"契约 + 黑板"协作模型与 AST 静态契约校验体系。
4. **安全与工程化**：掌握 Docker 容器隔离 + AST 静态扫描分级 + 运行时越权拦截（sitecustomize 注入）的多层防线设计；熟练 git 工作流、自动化测试（298 用例）与 golden 客观评测体系建设。
5. **前端可视化**：掌握 Vue 3 + TypeScript + Vite，基于 WebSocket 实时事件流实现多智能体运行过程的实时可视化与回放交互。

**技能印证（"基于 / 利用"强动词短语）**：

- 基于"契约 + 黑板 + 最小权限"协作模型，实现 10+ 角色多智能体并行编排与平台级接口契约校验（AST 缺口自动修复）。
- 基于 LLM 前缀缓存命中机制重构消息组织，结合阶段预算与全局熔断，将多智能体流水线输入成本降低约 65%。
- 利用 AST 静态扫描与 Docker / 宿主双模沙箱（运行期越权拦截），构建生成代码的多层执行安全与隔离体系。

## 【核心职责与成果】（5 个攻坚方向）

1. **主导设计** 多智能体编排引擎：从零自研 Agent ReAct 工具循环与 10 个工具的注册调度系统（按角色最小权限白名单硬约束，不依赖现成 Agent 框架），以声明式编排配置统一阶段重试、Token 预算与质检回跳/升级策略，实现多路编码、4 路审查智能体阶段内并行调度（单阶段 4–8 个智能体并发），以并行换取吞吐，单任务端到端交付收敛至 10–19 分钟。

2. **构建** 跨项目经验召回层：函数级经验三态标记（verified / has-issues / unreviewed，未查过≠通过），召回侧仅返回验证通过条目，形成"推理 + 经验召回"闭环，实测跨任务召回生效：设计阶段复用历史架构方案，修复环节按报错特征匹配历史修复经验。

3. **重构** 交付验证链路：搭建"静态 AST 契约门禁 + 动态测试门禁 + 预置客观行为断言"三级验证体系，将生成代码可运行交付率从 20% 拉升至 90%、质检通过率从 40% 提升至 80%；经覆盖多任务形态的 10 个端到端项目压测，门禁判定与客观验证结果**完全一致**（测试环境）。

4. **支撑** 成本工程体系：通过重构消息组织命中 LLM 前缀缓存（命中价约为 1/10），引入动态 Token 熔断机制，实现缓存命中率 72–80%、输入成本降约 65%，超额任务拦截从 6 次/轮**归零**，确保 LLM 成本 100% 可控可预算。

5. **搭建** 执行安全与容灾机制：Docker 容器隔离 + AST 静态扫描分级 + 进程级运行时越权拦截逐层防护（生成代码逃逸项目目录即被拒绝）；基于事件增量快照的断点续跑，保障异常中断后**秒级恢复上下文**，每轮产物 git 自动归档可回滚；独立完成 FastAPI/WebSocket 后端与 Vue3 实时可视化前端全栈交付（约 12.7k 行 Python + 6.7k 行前端），沉淀 298 个单元测试全绿。

---

## 附 A：术语对照表（面试口径，不粘进简历）

| 简历用语 | 大白话解释 | 面试可展开的技术细节 |
|---|---|---|
| 声明式流水线编排配置 | 用配置文件描述流水线每个阶段的顺序、重试次数、Token 预算、质检回跳/升级规则，引擎按配置执行，改配置不改代码 | 配置段在 `configs/default.json` 的 `pipeline_spec` 字段；升级条件用内置谓词枚举（不用 eval，防注入）；支持 minimal/default/full 三档消融评测 |
| 多路编码 / 4 路审查智能体并行 | 多个编码智能体同时写不同模块、4 个审查智能体同时审查，阶段内并行提速 | 并行 coder 按模块文件白名单隔离——互不可见、互不覆盖，接口错位由平台 AST 契约校验兜底 |
| 预置客观行为断言（golden test） | 预先写好的"标准答案"脚本：运行生成代码并核对行为是否符合预期，AI 自评不算数 | `benchmarks/verify/*.py` 固定断言；与平台质检判定交叉验证，实测抓出 3 处漏检/误判并修复 |
| 进程级运行时越权拦截 | 生成代码在宿主机运行时，注入一层拦截：凡是访问项目目录和系统临时目录之外的文件操作一律拒绝 | 利用 Python `sitecustomize` 启动钩子注入子进程，patch `os.remove/rename`、`shutil.rmtree/move` 等破坏性操作，逃逸即抛 PermissionError；静态扫描分不清"删项目文件"与"删系统文件"，故需运行时兜底 |

## 附 B：埋点对照表（面试准备用，正文无标记）

| # | 埋点句（正文原文） | 面试官可能的追问 | 可展开的深度 |
|---|---|---|---|
| 1 | "静态 AST 契约门禁"（要点 2） | 为什么用 AST 而非类型系统/字符串匹配？契约缺口怎么判定？ | `contract_gap_check`：解析 FunctionDef/ClassDef/Assign 等节点对比契约 exports；入口 `__main__` 接线检查；质检期复查缺口直接 FAIL 进回跳/升级 |
| 2 | "通过重构消息组织命中 LLM 前缀缓存"（要点 3） | 前缀缓存是什么？命中价差多少？怎么保证命中？ | DeepSeek 缓存命中价 ≈1/10；稳定 system prompt 置前、消息只追加不重排、已读拦截、压缩说明追加到消息尾部保住公共前缀、易变内容（run_id/时间戳）禁止进前缀 |
| 3 | "进程级运行时越权拦截"（要点 4） | 静态扫描为什么不够？拦截层是怎么注入的？ | sitecustomize 是 Python 启动自动加载的钩子；shim 生成 sitecustomize.py 注入子进程 PYTHONPATH，patch 破坏性文件操作，逃逸项目根/临时目录即抛 PermissionError |
| 4（备选） | "声明式流水线编排配置"（要点 1） | 为什么用配置而非硬编码？升级条件谓词怎么防注入？ | 内置谓词枚举（escalate_condition）、不用 eval；重排/自定义流水线不改代码；支持消融评测（minimal/default/full） |

## 附 C：数据口径备忘（防面试被问穿）

- **"首次编译通过率 90%"**：口径 = 8/8 评测任务质检 PASS 且评分 100（`results_all.json`）；10 任务集中 todo_api（REST+SQLite，高难度）曾有 2 次生成未跑通、被门禁正确 FAIL —— 这是"门禁有效"的正面证据，面试被追问时如实讲。
- **"收敛为零"**：限定词必须带 ——"经 10 任务端到端压测（测试环境）"。
- **复杂度参照物**：单阶段并发 4–8 个智能体；平台自身约 12.7k 行 Python + 6.7k 行前端；单任务 Token 消耗 57 万–240 万（成本工程的意义所在）。
